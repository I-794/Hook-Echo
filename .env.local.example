# NWS (api.weather.gov) requires a User-Agent that identifies your app and a contact.
# Requests without it can return HTTP 403. This is read server-side only.
NWS_USER_AGENT="Hook-Echo Severe Weather Dashboard (cplichterman@proton.me)"

# Optional: override the default map start (Milwaukee area).
# NEXT_PUBLIC_DEFAULT_LAT=42.96
# NEXT_PUBLIC_DEFAULT_LON=-88.01
